export interface Template {
  id: number;
  name: string;
  description?: string;
  imageUrl?: string;
  demoUrl: string;
  slug: string;
  price: number;
  lemonLink: string;
  created_at: string;
  updated_at: string;
}
